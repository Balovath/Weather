<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Погодный Отчёт</title>
    <link rel="stylesheet" type="text/css" href="style.css">
</head>
<body>
    <div class="container">
        <div class="weather-report">
            <p>В городе <%= @city %> <%= @date %> числа.</p>
            <p>Максимальная температура за день: <%= @tmax %>°</p>
            <p>Минимальная температура за день: <%= @tmin %>°</p>
        </div>
        <div class="weather-forecast">
            <% @hours.each do |hour| %>
                <div class="weather-forecast-item">
                    <div class="time"><%= hour["datetime"][0..4] %></div>
                    <div class="icon" data-weather="<%= hour["icon"] %>"></div>
                    <div class="temperature"><%= hour["temp"] %>°</div>
                </div>
            <% end %>
        </div>
    </div>
</body>
</html>
